import mongoose from "mongoose";

const companySchema = new mongoose.Schema({


    // here some logic
    // after 25 janwe compltete the project
    
 });

export const Company = mongoose.model("Company", companySchema);